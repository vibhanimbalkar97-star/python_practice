list = 
| Question               | Answer                                                                                |
| ---------------------- | ------------------------------------------------------------------------------------- |
| `sort()` vs `sorted()` | `sort()` modifies the original list; `sorted()` returns a new sorted list.            |
| List slicing           | Extracts part of a list using `list[start:end:step]`.                                 |
| List comprehension     | A concise way to create lists using `[expression for item in iterable if condition]`. |
| Copy a list            | `copy()`, slicing (`[:]`), `list()`, or `copy.deepcopy()` for nested lists.           |
| Check element exists   | Use the `in` operator.                                                                |
| Concatenate lists      | Use `+` to create a new list or `extend()` to modify the existing one.                |
| Question                  | Short Answer                                                                                                    |
| ------------------------- | --------------------------------------------------------------------------------------------------------------- |
| 31. Nested list           | A list containing other lists.                                                                                  |
| 32. Flatten list          | Convert nested lists into a single list using loops or list comprehension.                                      |
| 33. Multidimensional list | A list of lists used to represent tables or matrices.                                                           |
| 34. Memory allocation     | Lists store references to objects; assigning one list to another shares the same underlying list unless copied. |
| 35. Array vs List         | Lists are flexible and heterogeneous; arrays are homogeneous and more efficient for numerical data.             |
| 36. Time complexity       | Index access: O(1), append: O(1), search: O(n), sort: O(n log n).                                               |
| 37. List comprehension    | A concise and often faster way to create lists than using loops with `append()`.                                |
| 38. Mutable vs Immutable  | Lists are mutable; strings, tuples, and numbers are immutable.                                                  |
| 39. Unpacking             | Assign list elements to variables using `a, b = lst` or `*` for remaining elements.                             |
| 40. `zip()`               | Combines multiple iterables element by element into tuples.                                                     |

Coding Practice Questions
Find the largest element in a list.
Find the smallest element in a list.
Remove duplicates from a list.
Find the second largest number.
Count occurrences of an element.
Merge two sorted lists.
Rotate a list by k positions.
Find common elements between two lists.
Split a list into even and odd numbers.
Find the sum of all elements in a list.

Remove all empty strings from a list.
Find duplicates in a list.
Convert a list into a dictionary.
Find pairs with a given sum.
Shuffle a list randomly.
Generate a list of prime numbers.
Find missing numbers in a sequence.
Swap two elements in a list.
Convert a list of strings to integers.
Filter elements based on conditions.
===========================================================================================
tuple Questions
Beginner Level (1–10)
What is a tuple in Python?
How do you create a tuple?
What is the difference between a list and a tuple?
How do you access elements in a tuple?
How do negative indexes work in tuples?
How do you find the length of a tuple?
Can you modify a tuple? Why or why not?
How do you iterate through a tuple?
How do you check if an element exists in a tuple?
How do you concatenate two tuples?


Intermediate Level (11–20)
How do you slice a tuple?
How do you count occurrences of an element?
How do you find the index of an element?
How do you unpack a tuple?
What is tuple packing and unpacking?
What is the use of * (asterisk) in tuple unpacking?
How do you convert a list into a tuple?
How do you convert a tuple into a list?
How do you create a tuple with one element?
What happens if you forget the comma in a single-element tuple?

📌 Quick Revision Table
Q	Answer
1	Tuple = ordered, immutable collection
2	Create using ()
3	List is mutable, tuple is immutable
4	Access using indexing
5	Negative indexing starts from the end
6	len() gives the length
7	Tuples cannot be modified
8	Iterate using a for loop
9	Use in to check existence
10	Concatenate with +
11	Slice using [start:end]
12	Count with count()
13	Find position with index()
14	Unpack using a, b, c = t
15	Packing combines values; unpacking separates them
16	* collects remaining values during unpacking
17	Convert list → tuple with tuple()
18	Convert tuple → list with list()
19	Single-element tuple needs a comma: (10,)
20	Without the comma, (10) is an int


Coding Questions (21–30)
Find the largest element in a tuple.
Find the smallest element in a tuple.
Find the second largest element.
Count the frequency of each element.
Remove duplicates from a tuple.
Reverse a tuple.
Merge two tuples.
Find common elements between two tuples.
Find the sum of all elements.
Find the average of all elements.

Question	Solution
21	max() or loop
22	min() or loop
23	Track largest and second
24	count() with a visited list, or use a dictionary
25	Store unique values in a list, then convert to a tuple
26	t[::-1] or tuple(reversed(t))
27	Use + to concatenate tuples
28	Loop through one tuple and check membership in the other
29	sum() or accumulate with a loop
30	sum / len or compute both with a loop

Advanced Level (31–40)
What are nested tuples?
How do you access elements inside nested tuples?
Explain tuple immutability.
Why are tuples faster than lists?
When should you use tuples instead of lists?
Can a tuple contain mutable objects?
Explain tuple hashing.
Can tuples be used as dictionary keys? Why?
What is the memory difference between a tuple and a list?
Explain packing and unpacking with examples.


Scenario-Based Questions (41–50)
Swap two variables using tuples.
Return multiple values from a function using a tuple.
Find duplicate elements in a tuple.
Remove duplicate elements while preserving order.
Convert a tuple of strings to integers.
Convert a tuple into a dictionary.
Sort a tuple.
Find missing numbers in a tuple.
Rotate a tuple by k positions.
Filter elements based on a condition.

| Q  | Problem                     | Solution                               |
| -- | --------------------------- | -------------------------------------- |
| 51 | Swap variables              | `a, b = b, a`                          |
| 52 | Return multiple values      | `return x, y`                          |
| 53 | Find duplicates             | `count() > 1`                          |
| 54 | Remove duplicates           | Use a `unique` list and preserve order |
| 55 | Convert strings to integers | Loop + `int()`                         |
| 56 | Tuple → Dictionary          | `dict()` or `zip()`                    |
| 57 | Sort a tuple                | `tuple(sorted(t))`                     |
| 58 | Find missing numbers        | Check numbers in the expected range    |
| 59 | Rotate by `k`               | Slicing: `t[-k:] + t[:-k]`             |
| 60 | Filter elements             | Loop with an `if` condition            |



Tricky Interview Questions (51–60)
Why are tuples immutable?
Is an empty tuple truthy or falsy?
Can a tuple contain a list?
Can you modify a list inside a tuple?
Difference between () and (1,)?
Difference between tuple packing and tuple creation?
What happens when you multiply a tuple?
t = (1, 2)
print(t * 3)
Difference between tuple() and list()?
Can a tuple contain another tuple?
Why is a tuple more memory efficient than a list?

| Q  | Answer                                                                       |
| -- | ---------------------------------------------------------------------------- |
| 31 | Nested tuple = tuple inside another tuple                                    |
| 32 | Access with multiple indexes (`t[1][0]`)                                     |
| 33 | Tuples are immutable                                                         |
| 34 | Faster because immutable                                                     |
| 35 | Use for fixed data                                                           |
| 36 | Yes, tuples can contain mutable objects like lists                           |
| 37 | Hashable only if all elements are immutable                                  |
| 38 | Yes, immutable tuples can be dictionary keys                                 |
| 39 | Tuples use less memory than lists                                            |
| 40 | Packing combines values; unpacking separates them                            |
| 51 | Immutable by design for safety and efficiency                                |
| 52 | Empty tuple is falsy                                                         |
| 53 | Yes, tuples can contain lists                                                |
| 54 | You can modify the list inside a tuple, but not replace it through the tuple |
| 55 | `()` is empty; `(1,)` is a one-element tuple                                 |
| 56 | Packing (`1,2,3`) and tuple creation (`(1,2,3)`) create the same tuple       |
| 57 | `*` repeats the tuple                                                        |
| 58 | `tuple()` creates tuples; `list()` creates lists                             |
| 59 | Yes, tuples can contain other tuples                                         |
| 60 | Tuples are more memory efficient because they are immutable                  |

======================================================================================
sets 

1. What is a set in Python?
2. How do you create a set?
3. What is the difference between a set and a list?
4. What is the difference between a set and a tuple?
5. Can a set contain duplicate elements?
6. Is a set ordered or unordered?
7. Can you access elements using indexing?
8. How do you add an element to a set?
9. How do you add multiple elements?
10. How do you remove an element?

| Q  | Answer                                                              |
| -- | ------------------------------------------------------------------- |
| 1  | Set = unordered, mutable collection of unique elements              |
| 2  | Create using `{}` or `set()`                                        |
| 3  | List allows duplicates and indexing; set does not                   |
| 4  | Set is mutable and unordered; tuple is immutable and ordered        |
| 5  | No, sets automatically remove duplicates                            |
| 6  | Sets are unordered                                                  |
| 7  | No, sets don't support indexing                                     |
| 8  | Add one element with `add()`                                        |
| 9  | Add multiple elements with `update()`                               |
| 10 | Remove elements with `remove()`, `discard()`, `pop()`, or `clear()` |

{} creates a dictionary, not an empty set. To create an empty set, use:

s = set()
Use a set when:
You need unique values.
You want fast membership checks (x in s).
You need operations like union, intersection, and difference.
Remember:
add() → One element.
update() → Multiple elements.
remove() → Raises KeyError if the element doesn't exist.
discard() → Does nothing if the element doesn't exist.

11. Difference between remove(), discard(), pop(), and clear()?
12. How do you find the length of a set?
13. How do you check if an element exists?
14. How do you iterate through a set?
15. Can a set contain mutable objects like lists?
16. Can a set contain tuples?
17. How do you create an empty set?
18. Why does {} create a dictionary instead of a set?
19. What is a frozen set (frozenset)?
20. When should you use a set?

| Q  | Answer                                                                                                                 |
| -- | ---------------------------------------------------------------------------------------------------------------------- |
| 11 | `remove()` → error if missing; `discard()` → no error; `pop()` → removes an arbitrary element; `clear()` → removes all |
| 12 | `len()` returns the number of elements                                                                                 |
| 13 | Use `in` to check existence                                                                                            |
| 14 | Iterate with a `for` loop                                                                                              |
| 15 | No, lists are mutable and unhashable                                                                                   |
| 16 | Yes, tuples can be stored if all their elements are hashable                                                           |
| 17 | Create an empty set with `set()`                                                                                       |
| 18 | `{}` creates a dictionary, not a set                                                                                   |
| 19 | `frozenset` is an immutable set                                                                                        |
| 20 | Use sets for unique values, fast membership tests, and set operations                                                  |

Set methods that modify the set: add(), update(), remove(), discard(), pop(), clear().
Immutable objects like int, str, and tuples containing only immutable elements can be stored in sets.
Mutable objects like list, dict, and set cannot be stored in a set because they are unhashable.
frozenset is useful when you need the behavior of a set but the data should never change, or when you need to use a set as a dictionary key.

21. What is the union of two sets?
22. Difference between union() and |?
23. What is the intersection of two sets?
24. Difference between intersection() and &?
25. What is the difference of two sets?
26. Difference between difference() and -?
27. What is the symmetric difference?
28. Difference between symmetric_difference() and ^?
29. How do you update a set?
30. Difference between update() and add()?

| Q  | Answer                                                                                         |
| -- | ---------------------------------------------------------------------------------------------- |
| 21 | Union = all unique elements from both sets                                                     |
| 22 | `union()` and `\|` give the same result; `union()` also accepts other iterables                |
| 23 | Intersection = common elements                                                                 |
| 24 | `intersection()` and `&` give the same result; `intersection()` also accepts other iterables   |
| 25 | Difference = elements in the first set but not the second                                      |
| 26 | `difference()` and `-` give the same result; `difference()` also accepts other iterables       |
| 27 | Symmetric difference = elements in either set but not both                                     |
| 28 | `symmetric_difference()` and `^` give the same result; the method also accepts other iterables |
| 29 | `update()` adds multiple elements to a set                                                     |
| 30 | `add()` adds one element; `update()` adds multiple elements from an iterable                   |

Union (|) → Everything from both sets.
Intersection (&) → Only common elements.
Difference (-) → Elements only in the first set.
Symmetric Difference (^) → Elements in one set or the other, but not in both.

31. What is intersection_update()?
32. What is difference_update()?
33. What is symmetric_difference_update()?
34. What is issubset()?
35. What is issuperset()?
36. What is isdisjoint()?
37. How do you convert a list to a set?
38. How do you remove duplicates from a list using a set?
39. How do you convert a set into a list?
40. How do you sort a set?

| Q  | Answer                                                                                        |
| -- | --------------------------------------------------------------------------------------------- |
| 31 | `intersection_update()` keeps only common elements and modifies the original set.             |
| 32 | `difference_update()` removes elements found in the second set and modifies the original set. |
| 33 | `symmetric_difference_update()` keeps only non-common elements and modifies the original set. |
| 34 | `issubset()` checks if all elements of one set exist in another.                              |
| 35 | `issuperset()` checks if one set contains all elements of another.                            |
| 36 | `isdisjoint()` checks whether two sets have no common elements.                               |
| 37 | Convert a list to a set using `set()`.                                                        |
| 38 | Remove duplicates with `set()`, but remember it does not preserve order.                      |
| 39 | Convert a set to a list using `list()`.                                                       |
| 40 | Use `sorted()` to get a sorted list from a set.                                               |

41. Find the union of two sets.
42. Find the intersection of two sets.
43. Find the difference between two sets.
44. Find the symmetric difference.
45. Check if one set is a subset of another.
46. Check if one set is a superset of another.
47. Check whether two sets are disjoint.
48. Remove duplicate elements from a list using a set.
49. Find common elements between two lists using sets.
50. Find unique elements from two lists.
51. Find elements present only in the first set.
52. Find elements present only in the second set.
53. Find the total number of unique elements from two lists.
54. Find the missing elements between two sets.
55. Check if two sets are equal.
56. Find the maximum and minimum element in a set.
57. Find the sum of all elements in a set.
58. Filter even numbers from a set.
59. Convert a list into a set and back into a list.
60. Create a frozenset and explain why it is immutable.

| Q  | Solution                                                         |
| -- | ---------------------------------------------------------------- |
| 48 | `list(set(list1))` removes duplicates                            |
| 49 | `set(list1) & set(list2)` finds common elements                  |
| 50 | `set(list1) ^ set(list2)` finds unique (non-common) elements     |
| 51 | `a - b` gives elements only in the first set                     |
| 52 | `b - a` gives elements only in the second set                    |
| 53 | `len(set(list1).union(set(list2)))` counts total unique elements |
| 54 | `a ^ b` finds elements present in only one of the two sets       |
| 55 | `a == b` checks if two sets are equal                            |
| 56 | `max(s)` and `min(s)` find the maximum and minimum values        |
| 57 | `sum(s)` returns the sum of all elements                         |
| 58 | Loop through the set and use `add()` to collect even numbers     |
| 59 | `list(set(numbers))` converts a list to a set and back to a list |
| 60 | `frozenset()` creates an immutable, hashable set                 |


| Question                       | Answer                                                                                 |
| ------------------------------ | -------------------------------------------------------------------------------------- |
| Why are sets faster?           | They use a hash table for **O(1)** average lookup.                                     |
| Why no duplicates?             | Sets store only unique values based on hashing and equality.                           |
| Why no indexing?               | Sets are unordered.                                                                    |
| Search complexity              | **O(1)** average, **O(n)** worst case.                                                 |
| Can a set contain another set? | No, because sets are mutable and unhashable.                                           |
| Why is `frozenset` hashable?   | It is immutable, so its hash value doesn't change.                                     |
| `remove()` vs `discard()`      | `remove()` raises `KeyError` if the element is missing; `discard()` does nothing.      |
| When use a set?                | For unique values, fast lookups, and set operations.                                   |
| Why unordered?                 | Elements are stored by hash values, not by index.                                      |
| What is hashing?               | A process that converts an object into a fixed hash value for fast storage and lookup. |

===================================================================================================================================
dictionary